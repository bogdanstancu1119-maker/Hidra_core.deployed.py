name: PSIE Scut - Oglinda in Brate

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  psie_verificare:
    runs-on: ubuntu-latest
    steps:
      - name: Pasul 1
        run: echo "Verificare initiala OK"

      - name: Pasul 2
        run: echo "Scutul este activ si protejat"
        
